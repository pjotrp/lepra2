
%h1
  = @personal_history.person.name if @personal_history.person

%table
  = render(partial: "shared/attribute", locals: { :rec => @personal_history }, collection: @personal_history.fields )

%hr

= link_to 'Edit', edit_personal_history_path(@personal_history) 
|
= link_to 'Back', person_path(@personal_history.person)
